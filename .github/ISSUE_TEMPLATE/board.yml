---
layout: default
title: Board
---

<h2 style="color:white;">📌 AiX Lab Board</h2>
<p style="color:#ccc;">GitHub Issues 기반 게시판입니다.</p>

<script>
  fetch("https://api.github.com/repos/jinkimh/jinkimh.github.io/issues")
    .then(res => res.json())
    .then(data => {
      let html = "<ul>";
      data.forEach(issue => {
        html += `<li>
          <a href="${issue.html_url}" target="_blank">
            ${issue.title}
          </a> — ${issue.user.login}
        </li>`;
      });
      html += "</ul>";
      document.getElementById("issue-list").innerHTML = html;
    });
</script>

<div id="issue-list" style="color:white;">Loading…</div>

